{
    "name": "sortund-ai-worker",
    "version": "1.0.0",
    "scripts": {
        "format": "uv run black .",
        "test": "uv run pytest",
        "dev": "uv run uvicorn app.main:app --host 0.0.0.0 --reload --port 8000"
    }
}
