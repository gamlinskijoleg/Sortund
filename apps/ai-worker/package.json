{
    "name": "sortund-ai-worker",
    "version": "1.0.0",
    "scripts": {
        "format": "black .",
        "test": "pytest",
        "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --reload --port 8000"
    }
}
